const fs=require('node:fs'),path=require('node:path'),assert=require('node:assert/strict'),{execFileSync}=require('node:child_process');
const root=path.resolve(__dirname,'..');process.chdir(root);
fs.mkdirSync('www',{recursive:true});fs.writeFileSync('www/stale.js','stale');execFileSync(process.execPath,['scripts/build-web.cjs']);assert(!fs.existsSync('www/stale.js'));for(const name of ['tests','node_modules','assets/reference','README.md','assets/experiments/animals/ASSET-PROVENANCE.md'])assert(!fs.existsSync(path.join('www',name)),name);
let count=0;function check(dir){for(const name of fs.readdirSync(dir)){const file=path.join(dir,name);if(fs.statSync(file).isDirectory())check(file);else{assert.deepEqual(fs.readFileSync(file),fs.readFileSync(path.relative('www',file)));count++;}}}check('www');assert(fs.existsSync('www/js/native.js'));const config=JSON.parse(fs.readFileSync('capacitor.config.json'));assert.equal(config.webDir,'www');assert(!config.server?.url);console.log(`PASS ${count} generated files match canonical source; stale/dev/reference files excluded; local bundle config.`);
