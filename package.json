{
  "name": "makmal-cilik",
  "version": "2.5.0",
  "private": true,
  "description": "Makmal Cilik Android shell and web packaging",
  "scripts": {
    "test": "node scripts/test-all.cjs",
    "build:web": "node scripts/build-web.cjs",
    "verify:web": "npm test && npm run build:web",
    "sync:android": "npm run build:web && cap sync android",
    "android:debug": "build-android-debug.bat",
    "android:open": "cap open android"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "@capacitor/android": "8.5.2",
    "@capacitor/app": "8.1.1",
    "@capacitor/core": "8.5.2"
  },
  "devDependencies": {
    "@capacitor/cli": "8.5.2"
  }
}
